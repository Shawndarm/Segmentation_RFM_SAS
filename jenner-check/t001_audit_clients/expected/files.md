These URLs are produced by the live Jenner API and are tied to the run
that produced the snapshot — they expire when the run is reaped on the
server, so re-running the bundle regenerates them. The values below are
populated on the next API run.

## Datasets

Datasets created by this bundle (see log.txt for the full list).

## Files

Output listing and ODS plot specs produced by the run.
